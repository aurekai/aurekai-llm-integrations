# OpenAI Remote MCP Demo

Goal: GPT routes tool calls to Aurekai MCP and exports proof bundle.

```bash
npm run openai:demo
node providers/openai/run.mjs --dry-run --remote-mcp https://mcp.aurekai.ai
```
