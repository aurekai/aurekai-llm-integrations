# Gemini Context Cache Bridge Demo

Goal: Combine Gemini context cache ID with Aurekai semantic cache hash.

```bash
npm run gemini:demo
node providers/gemini/run.mjs --dry-run --cache-id gemini-cache-bridge-001
```
