# Anthropic Claude Desktop MCP Demo

Goal: Claude Desktop uses Aurekai MCP for artifact lineage inspection.

```bash
akai provider anthropic run --mcp-config ./providers/anthropic/manifest.json --proof
```
